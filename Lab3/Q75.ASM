DATA SEGMENT
NUM1 DD 98765432H
NUM2 DD 11111111H
SUM DD ?
CARRY DB ?
DATA ENDS
CODE SEGMENT
ASSUME CS:CODE,DS:DATA
PROGRAM:
    MOV AX,DATA
    MOV DS,AX

    LEA SI,NUM1
    LEA DI,NUM2
    LEA BX,SUM
    MOV CX,4H
    CLC

    BACK:
        MOV AL,[SI]
        SBB AL,[DI]
        DAS
        XCHG [BX],AL
        INC BL
        INC SI
        INC DI
    LOOP BACK

    MOV AH,4CH
    INT 21H
CODE ENDS
END PROGRAM