DATA SEGMENT
        NUM1 DW 0FFFFH
        NUM2 DW ?
DATA ENDS
CODE SEGMENT
ASSUME CS:CODE,DS:DATA
START:
        MOV AX,DATA
        MOV DS,AX
        MOV BX,NUM1
        MOV NUM2,BX
        MOV AH,4CH
        INT 21H
CODE ENDS
END START
